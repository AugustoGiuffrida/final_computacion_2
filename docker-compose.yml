# Levanta el sistema completo en una máquina: Redis, el servidor y los workers.
#
# El cliente NO está acá a propósito: es de quien usa el servicio, no parte del
# despliegue. Se ejecuta desde afuera contra localhost:9000.
#
#   docker compose up -d                  levanta todo
#   docker compose up -d --scale worker=3 tres workers, en caliente
#   docker compose logs -f worker         seguir lo que hacen
#   docker compose down                   bajar todo

services:
  # El intermediario entre el servidor y los workers. Cumple dos papeles con bases
  # distintas: la 0 es el broker (los mensajes "hay trabajo") y la 1 el result backend
  # (en qué anda cada tarea).
  redis:
    image: redis:7.4-alpine
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 3s
      timeout: 2s
      retries: 10

  # Atiende a los clientes. Lanza su propio proceso hijo de ingreso al arrancar.
  servidor:
    build: .
    command: python -m app.server --host 0.0.0.0 --port 9000
    # Las rutas van acá y no en el Dockerfile porque solo significan algo junto a los
    # volúmenes de más abajo, y `redis` es un nombre de servicio de ESTE archivo.
    environment:
      IMAGENES_STORAGE_DIR: /mnt/imagenes
      IMAGENES_DATABASE_DIR: /var/lib/final
      IMAGENES_BROKER_URL: redis://redis:6379/0
      IMAGENES_RESULT_BACKEND: redis://redis:6379/1
    ports:
      - "9000:9000"
    volumes:
      # Compartido con los workers: por la cola viajan rutas, no imágenes, así que los
      # dos tienen que ver el mismo sistema de archivos.
      - imagenes:/mnt/imagenes
      # La base es SOLO del servidor y su proceso hijo. Va en su propio volumen y no en
      # el compartido: SQLite desaconseja los sistemas de archivos de red, y además no
      # lo necesita porque quienes la usan viven en la misma máquina.
      - base:/var/lib/final
    depends_on:
      redis:
        condition: service_healthy
    restart: unless-stopped

  # Procesa las imágenes. Se pueden levantar tantos como se quiera, en caliente, y el
  # servidor ni se entera: los workers van a buscar trabajo a Redis, nadie se los asigna.
  worker:
    build: .
    command: celery -A app.worker.celery_app worker --loglevel=info
    environment:
      # Solo Redis. No lleva las rutas: no abre la base, y dónde escribir lo deriva de la
      # ruta del original que viene en la tarea.
      IMAGENES_BROKER_URL: redis://redis:6379/0
      IMAGENES_RESULT_BACKEND: redis://redis:6379/1
    volumes:
      # El mismo volumen que ve el servidor. No monta la base: no la necesita y no debe
      # tocarla — la regla del escritor único, hecha configuración.
      - imagenes:/mnt/imagenes
    depends_on:
      redis:
        condition: service_healthy
    restart: unless-stopped

volumes:
  # Las imágenes recibidas y sus resultados, sobre un sistema de archivos de red. Es lo
  # que permite que los workers corran en otra máquina: por la cola viajan rutas, no
  # imágenes, así que servidor y workers tienen que ver el mismo sistema de archivos.
  #
  # Las dos variables viven en `.env`, que no se versiona: son lo único que cambia de una
  # máquina a otra. Sin ellas compose se niega a arrancar y dice cuál falta, que es mejor
  # que montar en cualquier lado.
  #
  # Tres detalles medidos, que no conviene tocar:
  #
  #   - `addr` tiene que ser una IP. El montaje lo hace el demonio de Docker, que no está
  #     en la red de compose y no resuelve nombres de servicio.
  #   - Sin `nfsvers` usa NFSv3, que es lo que sirven tanto macOS como Linux. Solo hace
  #     falta forzar `nfsvers=4` contra un servidor que no ofrezca v3 —el caso de los
  #     servidores NFS en contenedor, que no traen el portmapper del puerto 111—.
  #   - El servidor NFS tiene que estar levantado ANTES. El volumen se monta cuando el
  #     contenedor se crea, y compose crea todos antes de arrancar ninguno: por eso un
  #     servidor NFS dentro de este mismo compose nunca llegaría a tiempo.
  imagenes:
    driver: local
    driver_opts:
      type: "nfs"
      o: "addr=${NFS_SERVIDOR:?la IP del servidor NFS; ver INSTALL.md},rw,noatime,nolock"
      device: ":${NFS_EXPORT:?el directorio exportado en el servidor NFS}"

  # La base de datos, en disco local del servidor. NO va por NFS: SQLite desaconseja los
  # sistemas de archivos de red —el bloqueo no es confiable y el modo WAL necesita memoria
  # compartida entre procesos— y no lo necesita, porque solo la tocan el servidor y su
  # proceso hijo, que viven siempre en la misma máquina.
  base:
