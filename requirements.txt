# Dependencias de ejecución, con la versión fijada: el mismo requirements da el mismo
# entorno acá y dentro del contenedor.
#
# La lógica de red no figura porque no necesita nada: argparse y asyncio vienen con la
# biblioteca estándar. Todo lo de abajo es procesamiento de imágenes o presentación.

# Salida con formato: tablas del historial, barras de progreso, colores. La usa solo el
# cliente; ningún módulo de red la importa, así que la capa de protocolo sigue siendo
# independiente de cómo se muestren las cosas.
rich==15.0.0

# Apertura y verificación de imágenes. La usa el proceso de ingreso para comprobar que lo
# que llegó sea realmente una imagen, y la van a usar los workers para procesarlas.
Pillow==12.3.0

# La cola de tareas distribuida: el servidor encola, los workers ejecutan. `[redis]`
# arrastra el cliente de Redis, que es el broker y el result backend.
celery[redis]==5.6.3

# Detección de caras. Se fija la versión 4: la 5 quitó `CascadeClassifier` y sus cascadas
# del paquete, y solo deja un detector que exige bajar un modelo aparte. `headless` evita
# las dependencias de interfaz gráfica, que un worker no necesita.
opencv-python-headless==4.14.0.94
