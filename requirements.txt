# Dependencias de ejecución.
#
# La lógica de red del cliente y del servidor funciona solo con la biblioteca
# estándar: argparse y asyncio vienen con Python. Las dependencias externas de
# procesamiento (Pillow, OpenCV, Celery, Redis) se agregan cuando se implementen
# los componentes que las necesitan.
#
# La de acá abajo es exclusiva de la PRESENTACIÓN del cliente. Ningún módulo de
# red la importa, de modo que la capa de protocolo sigue siendo independiente de
# cómo se muestren las cosas.

# Salida con formato: tablas del historial, barras de progreso, colores.
rich==15.0.0
